// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebaseConfig: {
        apiKey: 'AIzaSyCTJb64a4EOO8USrG3Pd0epQ1piWD3iFl4',
        authDomain: 'travel-agency-app-server.firebaseapp.com',
        databaseURL: 'https://travel-agency-app-server.firebaseio.com',
        projectId: 'travel-agency-app-server',
        storageBucket: 'travel-agency-app-server.appspot.com',
        messagingSenderId: '238237723840',
        appId: '1:238237723840:web:a08e48d185fc7bf8da5125',
    },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
